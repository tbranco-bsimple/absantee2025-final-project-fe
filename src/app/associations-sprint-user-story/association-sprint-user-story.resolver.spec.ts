/* import { TestBed } from '@angular/core/testing';
import { ResolveFn } from '@angular/router';

import { associationSprintUserStoryResolver } from './association-sprint-user-story.resolver';

describe('associationSprintUserStoryResolver', () => {
  const executeResolver: ResolveFn<boolean> = (...resolverParameters) => 
      TestBed.runInInjectionContext(() => associationSprintUserStoryResolver(...resolverParameters));

  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

  it('should be created', () => {
    expect(executeResolver).toBeTruthy();
  });
});
 */