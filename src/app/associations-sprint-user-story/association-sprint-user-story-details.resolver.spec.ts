/* import { TestBed } from '@angular/core/testing';
import { ResolveFn } from '@angular/router';

import { associationSprintUserStoryDetailsResolver } from './association-sprint-user-story-details.resolver';

describe('associationSprintUserStoryDetailsResolver', () => {
  const executeResolver: ResolveFn<boolean> = (...resolverParameters) => 
      TestBed.runInInjectionContext(() => associationSprintUserStoryDetailsResolver(...resolverParameters));

  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

  it('should be created', () => {
    expect(executeResolver).toBeTruthy();
  });
});
 */